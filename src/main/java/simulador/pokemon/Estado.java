package simulador.pokemon; 
/**
 * clase estado, se asigna el estado (Normal) para todos los pokemones
 * es una clase que esta abierta al cambio, abierta a implementarse en un futuro.
 * 
 */
public enum Estado { //O(1)
    NORMAL;
}
